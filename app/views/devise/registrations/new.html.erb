<div class="container">
  <div class="row">
    <div class="col-2"></div>
    <div class="col-8">
      <div class="row border-bottom text-center">
        <div class="col text-left mt-2">
          <h2>Qiitaへようこそ!</h2>
          <h2>新規登録(無料)して利用を開始しましょう。</h2>
        </div>
      </div>
      <div class="row mt-2">
        <div class="col-6">
          <div class="btn-group-vertical row">
            <button type="button" class="btn btn-dark btn-lg btn-block rounded">
              <i class="fab fa-github-square">
                GitHub アカウントで登録
              </i>
            </button>
            <button type="button" class="btn btn-primary mt-3 btn-lg rounded">
              <i class="fab fa-twitter-square">
                Twitter アカウントで登録
              </i>
            </button>
            <button type="button" class="btn btn-danger mt-3 btn-lg rounded">
              <i class="fab fa-google">
                Google アカウントで登録
              </i>
            </button>
          </div>
        </div>
        <div class="col-6 border-left form-group">
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field mb-4">
            <%= f.email_field :email, autofocus: true, autocomplete: "email",placeholder:"Eメール",class:'form-control' %>
          </div>

          <div class="field mb-4">
            <%= f.text_field :username , autofocus: true, autocomplete: "email",placeholder:"ユーザー名",class:'form-control' %>
          </div>

          <div class="field mb-4">
            <%= f.password_field :password, autocomplete: "new-password",placeholder:"パスワード",class:'form-control' %>
            <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
            <% end %><br />
          </div>

          <div class="field mb-4">
            <%= f.password_field :password_confirmation, autocomplete: "new-password",placeholder:"パスワード",class:'form-control' %>
          </div>
          <div class="actions">
            <%= f.submit "登録" ,class:'btn btn-primary btn-lg col-12' %>
          </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-2"></div>
  </div>
</div>